TODO
====

-- Add command-line options. At least:
   --help
   --version
   --no-prettyprint     (default: off)
   --indent=<string>    (default: "  ")
   --max-width=<number> (default: 80)
   --output=<path>      (default: - /stdout/)
   + filename to read   (default: - /stdin/)

-- Use some specialized pretty-print library.
   Lua-Nucleo dependency is too heavy for this tool.
